#terms
repository(repo) (github_vn)
#commands
- git init ( có thể sử dụng được git sau khi đánh lệnh này)
- git status (có thể thấy được trạng thấy dự án của mình , thay đổi j mình đều thấy được)
- git add ( cho phép mình lưu lại file trên dự án
)
-git add.(cho phép chuẩn bị lưu lại tất cả file)
-git reset (lấy lại những file chuẩn bị lưu)
-git commit (ghi chú trong git)
-git log (co lại tất cả những j mình commit )
-git log --oneline ()
-git checkout id (trợ lại commit hiện tại)
-git branch (xem coi mình đang ở nhánh nào )
-git checkout -b (branch name)
-git merge {branch name}
-git branch -d (name) ( xóa một nhánh)
